Algorithm Heap()
  H <- H[]
  lastNode <- 0
Algorithm insertElement(e)
  lastNode <- lastNode + 1
  H[lastNode] <- e
  upheap(lastNode)


Algorithm removeMin()
  H <- H[]
  H[0] <- H[1]
  H[1] <- H[lastNode]
  H[lastNode] <- null

  downheap(H[1])
  return H[0]

Algorithm upheap(p)
  while
