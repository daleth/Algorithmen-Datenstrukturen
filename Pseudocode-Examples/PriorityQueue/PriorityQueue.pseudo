Algorithm PriorityQueue()
c <- 0 // Zähler, der die Priority hochzählt. Jedes neue Element erhält eine höhere Nummer...
PQ <- new Heap()

Algorithm enqueue(e)
  c <- c + 1
  PQ.insertItem(c,e)

Algorithm dequeue()
  return PQ.removeMin()
